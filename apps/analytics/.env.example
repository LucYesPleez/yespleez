# YesPleez Analytics v2 — server-side configuration.
# Copy to .env (which is gitignored). The service-role key NEVER leaves
# this process: no client bundle, no route response, no log line.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# ANALYTICS_PORT=4100
