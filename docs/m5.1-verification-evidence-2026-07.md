# M5.1 Verification Evidence — Display-Join Parity

**Status:** COMPLETE — M5.1 accepted 2026-07-12. Commits `0563717` (D1/D2 EventScreen), `dcad2c3` (D3/D4/D7 enquiry family), `515f443` (D5/D6/D8 follows family) + this evidence commit, branch `v2-react`.

Each family was verified independently before its commit, per the approved plan. Verification ran live in the signed-in 5-profile test account (port 5173) against production data, with a guest cross-check on the isolated server (port 5199). No production rows were created, modified, or deleted. All eight in-scope display joins now resolve `profiles` by the source row's profile id, with the legacy join retained per-row for entries without one; renderers untouched; every map keyed by its renderer's pre-existing lookup key.

---

## Family 1 — EventScreen (D1 slot socials, D2 memberProfiles) — `0563717`

| Check | Before | After | Pass |
|---|---|---|---|
| Slot list (event `257038af…`, host view): names + genre lines | Dusky Waters "Indie Rock"; BVP "Smooth and criminal"; Heffekt; Daddy Longlegs "Junglistic breaks, 140, dnb etc"; Farmer Bwoi (screenshot capture, pre-change) | identical, all five | ✅ |
| Expanded single-type SlotCard (Daddy Longlegs) | genre pills "Drum & Bass · Breaks · Dubstep / Bass · Jungle", FOLLOWING, VIEW PROFILE, OFFERED, host controls | identical | ✅ |
| Lineup tab (memberProfiles renderer): all member cards render | list rendered via `user_id` join | list renders via `artist_profile_id` join; profile-named entries unchanged (e.g. "Bwoi" — the profile name, as before) | ✅ |
| **Option (a) record — the multi-type owner's cards** | **undefined behaviour**: the `user_id` join had no type filter and last-row-wins map assembly, so the displayed enrichment for a multi-profile owner was database-row-order **nondeterministic** | **deterministic**: the booked profile's own data — the Dusky Waters lineup card shows BAND / Bellingen / Indie Rock, i.e. exactly the band profile the lineup row's `artist_profile_id` names | ✅ (replaces undefined behaviour, not defined behaviour — recorded per approval) |

One implementation correction during this family (caught by vite before any commit): the new map variable collided with a pre-existing `membersById`; renamed to `mProfById`/`mProfByUid`.

## Family 2 — enquiry family (D3 VenueDashboard, D4 ArtistDashboard offers, D7 EnquiryCard) — `dcad2c3`

| Check | Before | After | Pass |
|---|---|---|---|
| VenueDashboard enquiry cards (both organic rows) | applicant resolved via `user_id`+type key-pair | SHORTLISTED card: "Lucious / DJ / Bellingen / Multi genre low slung hooners."; ACCEPTED card: "Dusky Waters / BAND / Bellingen / Indie Rock" — names/types/locations/bios identical, resolved via `applicant_profile_id` | ✅ |
| ArtistDashboard INCOMING offers | venue resolved via `venue_user_id`+`type='venue'` | "Elbows Rest / VENUE / Underground live studio forest vibes / SHORTLISTED" — identical card, resolved via `venue_profile_id` | ✅ |
| EnquiryCard per-card fetch (D7) | fetch by `applicant_user_id`+type when no pre-batched profile | exercised by the offers panel (offers pass no pre-batched profile); cards rendered fully | ✅ |
| Type-label note | cards show HEAD's pre-existing labels (e.g. "DJ / PRODUCER" in EnquiryCard) | identical — the label rebrand belongs to the uncommitted feature work and was deliberately not touched | ✅ (parity, not endorsement) |

## Family 3 — follows family (D5 MyScene map+avatars, D6 three following lists, D8 dayArtists) — `515f443`

| Check | Before | After | Pass |
|---|---|---|---|
| ArtistDashboard FOLLOWING (baseline captured pre-change in the same session) | 5 rows: Anti-Faffist/Lismore, Social capital/Byron Bay, Pokki, The Bellingen Brewing Co, Daddy Longlegs/Cairns | identical five rows, count 5, resolved via `target_profile_id` | ✅ |
| HostDashboard / VenueDashboard following lists | same source rows, same pattern | same pattern applied (identical code shape to the verified ArtistDashboard list; same five follows render) | ✅ |
| MyScene followed profiles + updated-follows strip | map keyed `user_id` fed by `user_id` join | all five names render; map now keyed `f.entity_id` (the renderer's actual lookup key — §3.2 discipline), `updatedFollows` computed over the same combined row set | ✅ |
| MyScene avatar lazy-load | fetch by `user_id` list | fetch by resolved profile ids; `followAvatars` still keyed by `user_id` (consumers unchanged) | ✅ |
| D8 dayArtists avatars | `artist_id` join | partitioned `artist_profile_id`/`artist_id` join — the identical pattern verified in D1/D2/D5; MyScene renders cleanly | ✅ (pattern-verified; the dayArtists panel's *links* are uncommitted feature work, adjusted in the working tree to stay consistent) |
| Punter-preference dedupe | applied to all fetched rows | retained on the legacy partition only — an id names exactly one row, so the id partition cannot need it; output set identical for all current data (every backfilled target is the non-punter row the old dedupe picked) | ✅ |

**Disclosed consequence (URL-adjacent, not a URL change):** followed-profile rows now carry `id` (the byId map requires fetching it), so the following-list PortraitCards — which already navigated pre-M5.1 via their legacy fallback — now emit canonical `/profile/<id>?type=` URLs. Same destinations; previously reached through the M5 redirect shim; this is M5's intended end-state arriving via data availability, not a new link surface.

## Cross-family regression

- **X1 screens untouched and unchanged:** ApplicationsScreen and the HostDashboard applications/lineups tabs still join `applications.artist_id` (no profile column exists — Booking-review boundary); no diff touches them.
- **X2 untouched:** SlotCard follow state read/write pair unchanged (deferred to M6); its FOLLOWING state rendered correctly throughout Family-1 verification.
- Console: zero M5.1-attributable errors across all touched screens in both sessions; only the pre-existing React style-shorthand warning (old timestamps).
- Ordering: every list's order still derives from the relationship-row queries (untouched); map assembly iterates fetch results exactly as before.
- Permissions: `profiles` public-read (`true`) filters identically by `id` or `user_id`; the RLS-scoped relationship queries are byte-identical apart from added select columns (`target_profile_id`, `artist_profile_id` — both public-readable columns on already-authorized rows).
- Requirements held: no schema, RLS, write-path, URL-surface, claim-workflow, or business-logic changes; no unrelated fixes (the EnquiryCard label and the follow `entity_type` bug remain exactly as they were).

## Rollback (unused)

`git revert 515f443 dcad2c3 0563717` — code-only, no database dependency in either direction; the legacy columns all eight sites stopped preferring remain present and dual-written until M8. Not exercised.

## Working-tree note

All eight sites live in files carrying unrelated uncommitted feature work; every commit staged only the M5.1 hunks via index-only staging (HEAD-copy method), verified by `git diff --cached --stat` before each commit. The working tree's feature variants of the same regions were updated in place so the feature work remains consistent with the new join pattern when it eventually lands.
