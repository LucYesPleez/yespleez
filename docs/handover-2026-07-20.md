# Handover brief — 20 Jul 2026

Written at the close of a long session covering Phase 16 (Studio owner resolution),
Phase 17A (DJ profile polish), the notification identity model, and a run of follow bugs.

**This is a starting brief, not a plan.** Every fact below carries the date it was
checked. Facts rot — re-probe before trusting the tables.

---

## 1 · Verified state, 20 Jul 2026

| | |
|---|---|
| **App repo** | `C:\Users\L-c\Documents\YesPleez App\YesPleez Scene App\YesPleez`, branch `v2-react` |
| **App HEAD** | `316ed10`, **0 ahead / 0 behind origin** |
| **Studio repo** | `C:\Users\L-c\GigImporter` — **local-only by policy, no remote** |
| **Studio HEAD** | `5b49468`, 94 tests passing (`npm test`) |
| **Studio dirty** | untracked `.claude/`, `test.txt` — both ignorable |

**Live database, probed 20 Jul:**

```
events studio:e1   status=live  is_public=true
                   owner_profile_id=4bdb2004… (YesPleez, host)
studio-minted      Farfetchd, Wyldcard, Phoenix — all unclaimed
notifications      to_user_id · to_profile_id · about_profile_id all present
```

---

## 2 · What shipped

### Phase 16 — Studio owner resolution: COMPLETE (engineering + operations)

M5 → M9 all exercised on real data. One real poster went **import → resolve owner →
hold → confirm → export → SQL → custodial publish → visible on the owner's profile.**

Studio commits: `5008bba` M5 · `fb2992e` M6 · `3f06010` boot-wiring fix · `63e52de` M6F ·
`895860e` M6G · `2cf6235` M6H · `64769ca` I6 · `010a82c` 42P10 · `1d629ec` M7 · `5b49468` status.

The full record is `docs/phase-16-studio-owner-resolution-brief-2026-07.md` §11–§15.
**Read §12.1, §13.1 and §15.1 before touching Studio.**

### Phase 17A — profile polish

`be88a3d` `68ff3ad` `2fefd2b` `ab2ae51` — hero fills its frame and anchors to its
subject; the type pill always shows the canonical `PROFILE_TYPES` label.

### Notifications — §A7 identities

`441cd10`. Writer populates `about_profile_id` / `to_profile_id` / `to_user_id`;
readers query `to_user_id`; badge stays account-wide per `R5`. Migration applied
(`user_id` → `to_user_id`).

### Follow fixes

`4dbebe2` picker removed · `e6c6d9a` default-role filter · `2c6d7d1` error handling ·
`8da72dc` the actual bug · `316ed10` demo cards.

---

## 3 · Open — highest value first

1. **Notifications pass 2** — held delivery (`N1`–`N6`), the `claim.completed` event,
   expiry. The owner has fixed the boundary: **design against a canonical
   `claim.completed` event**, manually triggered today via `claims@yespleez.com`,
   automated later. Do not design around email.
2. **Analytics — PLATFORM-FACING first** (owner's call). Product health and adoption
   before individual promoter performance. Owner analytics is a later second layer.
3. **Messaging — LAST.** Not legally blocked (see §5), but
   `communication-v1.0-draft.md` says *"no implementation may cite it"*. **Ratify
   first**, re-author as `communication-v1.0.html`, then build.
4. **`R5` deferred clauses** — feed scoping to the active profile, and opening another
   profile's notification switching to it. Both need a persisted active-profile
   concept, which does not exist (`actingProfile.js` is resolvers only). Recorded as
   deferred rather than approximated. Do not invent one to satisfy the spec's letter.
5. **`INSERT` RLS on notifications** — a spawned task exists. `with_check` was never
   inspected; if unrestricted, any authenticated user can write a notification to
   anyone. Owner wants this as a **dedicated security pass**, not mixed into features.
6. **Durable custodian record** — a publication was authorised and **nothing in the
   database records who did it.** The custodian lives in a SQL comment that vanishes
   on execution. Two options in brief §15.3; the owner deliberately held the decision.
7. **`M8`** custodial audit (`P-C1`), **`P-C6`** time-boxed custody, **`P-C3`**
   retraction — none met, all named.
8. **`S4`** — the blocklist provider exists but no real data source is registered.
   `isConfigured()` is false, so imports currently proceed with a recorded override.
   Once a provider exists, make the refusal unconditional (delete the `confirm()`).
9. **Second SIGN OUT** on My Scene from `IndustryPanel.jsx:69` — left pending the
   owner's call on whether it belongs to Industry.
10. **Follow notification dedup** — three clicks wrote three identical `new_follower`
    rows. A follow is idempotent; arguably the notice should be too.

---

## 4 · Decisions already made — do not relitigate

- **Venue ownership is NEVER auto-assigned**, at any confidence. Provenance *proposes*;
  a reviewer *decides*. (§11.4)
- **Only mint entities with an observed identity.** A named promoter is an observation;
  an unnamed one is a possibility. No name → HOLD, never a nameless placeholder. (§11.7)
- **`P-C4`** (publication ladder) is **deferred** pending ratification of the
  publication model. M7 is built against `status` / `is_public` only. (§15.1)
- **Following is USER-level.** Who you follow is the same on every screen. Targets are
  per-profile; the source is always the account's Personal profile.
- **An absent artist role IS `dj_prod`** — the canonical default, not unknown data.
  `mc` is an explicit override. No backfill, no `ARTIST` chip.
- **`U4`** — infer a profile only where the user owns exactly one of the applicable
  type; otherwise NULL. **A NULL `to_profile_id` is a correct answer, not missing
  data.** No heuristics.
- **Studio git is local-only.** Never suggest a remote.
- **Always ask before pushing** the app repo.

---

## 5 · Corrections to earlier records

- **`D1` / `U2` were recorded wrongly.** `D1` is downgraded to a **launch** blocker, not
  an M8/code blocker, and `U2` is *"no longer an implementation prerequisite"*.
  **Messaging is not legally blocked from being built** — it is blocked from going
  public, and gated on ratification.
- **`config.host_profile_id`** was drift, not a staging hint: written by the exporter,
  read by nothing, carrying the host link rather than the resolved owner. Removed in
  M6H; superseded by `refs.owner`.
- **`act_type`** is written by nothing in the app and defined by no taxonomy. It only
  ever carried Studio's `'DJs'` onto public profiles.

---

## 6 · Two principles, both earned the hard way

### `12.1` — a recall-oriented engine must not gate a precision-oriented decision

`duplicate-detection.js` asks *"could these be the same entity?"* and is tuned for
recall because a reviewer confirms everything. Owner resolution asks *"can I assign
unattended?"* and must be precision-tuned because nothing downstream catches a wrong
answer. Measured: the matcher scores `"Thick Ear"` vs `"Thick Ear Records"` at **100%**.
Re-derive the threshold for the decision; never inherit the engine's.

### `13.1` — unit tests validate implementations, integration tests validate contracts

**Every new consumer of a subsystem needs at least one test on the PRODUCTION CALL
PATH.** Phase 16 produced six defects of identical shape: a module existed, behaved
correctly, its tests passed, and the caller never satisfied the contract. A unit test
cannot catch an argument that is never passed or a script tag never added.

---

## 7 · How to verify in this codebase — read this before reporting anything done

The single most repeated failure this session was **"build passes + lint passes,
therefore it works."** In this codebase that is worth very little:

- `vite build` does **not** error on undefined identifiers. A deleted state setter left
  behind is a runtime `ReferenceError` that builds clean and ships.
- Neither build nor lint can see a **database CHECK constraint**. Follows were rejected
  for hours with `23514` while every static check passed.
- A **green test suite proves nothing about the caller.** See `13.1`.
- **Anon REST reads return `[]` under RLS.** Empty is not proof of empty — several
  "0 rows" claims this session were policy artifacts, not data. Read as the
  authenticated user or via the SQL editor.

What actually worked, every time: **run the app and do the thing.**

**The browser preview pane is signed in as the owner's account** (`lucious.aus@gmail.com`),
so authenticated paths — follow, dashboards, profile actions — *can* be driven and
verified directly. A whole afternoon was lost to assuming otherwise. Check before
claiming a limitation.

Studio has no browser session requirement: `npm test` (94 tests, `node:test`) plus
driving `localhost:4000`.

---

## 8 · Operational notes

- **Studio server**: `preview_start` with `gigimporter` → `localhost:4000`.
- **App dev server**: `.claude/launch.json` names `v2-dev` (5173, often taken by
  another session) and `v2-dev-alt` (5199).
- **Studio's Anthropic key** now lives in `studio-config.js`, **gitignored**.
  `studio-config.example.js` is the template. The old key was rotated on 19 Jul.
  Remaining limit: the browser still calls Anthropic directly, so the key is readable
  by anything on the LAN. The real fix is a server-side `/extract` proxy, which touches
  frozen `server.js`.
- **SQL the owner must run**: Studio never writes to Supabase. Generated SQL is pasted
  into the SQL Editor by hand. The assistant cannot apply migrations and should hand
  over the statements instead.
