# Soak / Polish Backlog — post-M5.1, pre-M6

**Status:** ACTIVE. The identity migration is paused at M5.1 (2026-07-12); M6 does not begin until explicitly resumed. This document is the single prioritized backlog for the soak period: real-user testing across every profile type, with each reported issue investigated fresh and classified before any action.

**Triage taxonomy** (assigned after investigation, never assumed):
1. **BUG** — genuine defect
2. **POLISH** — UX/polish improvement
3. **DEBT** — technical debt
4. **REGRESSION** — traced to M5/M5.1 specifically
5. **M6-BLOCKER** — blocks the write cutover or risks data integrity → the only class eligible for immediate fixing during soak
6. **SECURITY** — a live defect in production authorization (RLS/grants). **Added 2026-07-17.** Not an enhancement, not an M6 dependency: it is remediated **on its own schedule, independent of the migration**. The pause does not apply to it, and neither does "wait for M6" — M6 is not its fix.

**Action rule:** M6-BLOCKER and data-integrity items get fixed immediately (investigated → atomic fix → verified → isolated commit). **SECURITY items are scheduled on their own, independent of the migration's state.** Everything else is logged here and waits.

**Testing-environment note:** the dev servers serve HEAD **plus the uncommitted feature work** (enquiry flow UI, DJ/PROMOTER rebrand, profile-form refactor, dashboard polish across ~20 files). An issue seen while browsing may live in the uncommitted layer, not in HEAD — classification must say which.

---

## Pre-seeded from the migration's known-issues records (already investigated)

| # | Item | Class | Where tracked | Notes |
|---|---|---|---|---|
| S1 | Profile FOLLOW button silently fails for everyone — insert sends `entity_type: 'profile'`, rejected by CHECK constraint, error unchecked | BUG (pre-existing, high) | `known-issues/profile-follow-entity-type-drift.md` | Post-M5 the natural fix is one line (write the loaded profile's real type) + error handling. Worth early polish: follows are a core loop. Not an M6 blocker per se, but M6's write cutover touches this exact insert — fixing it *before* M6 keeps M6 clean. |
| S2 | Venue availability invisible to non-owners — no public-read policy; the CHECK AVAILABILITY calendar and enquiry picker render empty for exactly their audience | BUG blocking the (uncommitted) enquiry feature | `known-issues/enquiry-feature-rls-gaps.md` (F2) | Needs a product decision (public vs authenticated-only read). RLS change = its own isolated migration when decided. |
| S3 | Applicant accept/decline on enquiries is a silent no-op — UPDATE policy is venue-side only; UI optimistically lies | BUG (pre-existing) | same doc (F3) | Business-rule decision (should applicants write status?) — flagged for the enquiries/Booking rework. |
| S4 | Venue-initiated invites (InviteSheet) can never insert — policy requires `auth.uid() = applicant_user_id` | BUG (pre-existing, diagnosed) | `known-issues/venue-enquiries-schema-drift.md` (F4) | Same business-rule cluster as S3. Also schema-drift fields on that payload. |
| S5 | `artist/band/standup_availability` have no `profile_id` columns | DEBT / **M6-prerequisite** | M4 review §6 (F5) | Needs an M1-style additive column + M3-style backfill before M6 can cut availability writes for those types — or an explicit decision they stay account-keyed. Resolve during soak planning so M6 isn't blocked on arrival. |
| S6 | `applications` has no profile columns; all its joins/authz stay account-keyed | DEBT (architecture boundary) | Booking review (deferred since Fix 2/3) | Not eligible for M6; the Booking architecture review owns it. |
| S7 | Artist self-inserted/self-updated application statuses (interim RLS latitude) | DEBT | Fix 2/Fix 3 migration comments | Booking review. |
| S8 | Any authenticated user can insert notifications into any inbox | DEBT (accepted interim) | `known-issues/notifications-schema-drift.md` | Notifications authorization redesign. |
| S9 | "APPLYING AS: <wrong profile>" for multi-role accounts on Apply flow | BUG (pre-existing, small) | applications-schema-drift doc | Cosmetic-ish; multi-type accounts only. |
| S10 | EnquiryCard type labels at HEAD still say "DJ / PRODUCER" (rebrand lives in uncommitted feature work) | POLISH | uncommitted-work layer | Resolves when the feature work lands; don't fix twice. |
| S11 | React style-shorthand console warning (`background`/`backgroundClip`) on gradient headers | POLISH (console hygiene) | observed throughout M5 verification | Trivial, batch with other polish. |
| S12 | DiscoverScreen duplicate-key warning | POLISH — possibly already resolved by M5's `id` keys | M5 review §7 | Re-check during soak before closing. |
| S13 | Supabase org shows OUTSTANDING INVOICES warning | OPS | flagged 2026-07-11 | Not code. Clear before M6's production work. |
| S14 | The uncommitted feature work itself (~20 files) needs review, verification, and landing as its own change set | DEBT (process) | working tree | Landing it during soak shrinks every future index-staging dance and makes soak findings unambiguous. Recommended early. |

## Soak findings (append as reported)

| # | Reported | Investigation | Class | Disposition |
|---|---|---|---|---|
| S15 | My Scene: "Add to your scene" popup is cut off by the bottom nav bar | Confirmed pattern — no global safe-area convention exists for bottom-anchored overlays relative to `BottomNav`/mini player. This is the concrete trigger for the safe-area rule (see global polish pass below). | BUG (layout) | **DONE (2026-07-12)** — fixed via the global safe-area pass: `--yp-safe-bottom` CSS var (index.css), kept live by `BottomNav`'s `ResizeObserver` and the mini-player mount in `App.jsx`, consumed by every bottom-docked sheet/modal across the app. |
| S16 | DJ portrait card shows "DJ / PROMOTER" instead of "DJ / PRODUCER" | `profileTypes.js` centrally defines the artist label as "DJ / PROMOTER" (memory: `feedback_artist_label.md` — this is an intentional, previously-ratified rebrand, not a bug). PortraitCard/ProfileCard consume it centrally, so it's consistent everywhere at HEAD. | NOT A BUG — working as designed | No action. If the rebrand itself needs revisiting, that's a product decision, not a polish fix. |
| S17 | Default images aren't showing for demo/seed roles | `v2/public/default{dj,band,mic,promoter,venueblur}.*` all exist on disk and `ProfileScreen.jsx`'s `PLACEHOLDER_HERO` map references them correctly for **unclaimed profile heroes**. If the report is about a different surface (e.g. dashboard cards for demo/seed accounts with no avatar), that's a separate fallback path that may not have the same default wired in. | UNCONFIRMED — needs a live repro (which screen/card, which role) | Logged; ask for a screenshot or exact screen next time it's hit. |
| S18 | Past Events should have a filter system | No filter exists on Past Events tabs today (dashboards' events sections just list). | POLISH | **DONE (2026-07-12)** — new shared `PastEventsSearch` component (+ `filterPastEvents` helper) wired into every Past Events surface: `EventsSection` (Venue/Host dashboards), `ArtistDashboard`, `ProfileScreen`, `MySceneScreen`. |
| S19 | Add the same portrait/list toggle to the "Following" section that Discover-style lists have | Need to confirm which toggle the user means (Discover's grid/list view switch) and whether Following sections have layout space for it. | POLISH | Logged for a dedicated small pass; not part of the 3-item authorized polish pass. |
| S20 | Featured event genre pills should use the GlowPill gradient | `reference_glowpill.md` (memory) documents the GlowPill gradient token (`.genre-pill`/`.ytx-chip.on`) as the intended shared style. Featured-event pills likely use a plain/flat style instead. | POLISH | Logged; small isolated CSS-token pass, not in this authorized batch. |
| S21 | Edit Event: "SET TIMES COMING SOON" shows even when the host turned off "Set times needed" at creation | **Confirmed by code reading** — `EventScreen.jsx:815` gates the "SET TIMES COMING SOON" punter message purely on `!showTimesPublicly`, with no check for whether the event has any set-times content at all (`config.days` is `[]` when `setTimesNeeded` was off at creation, per `CreateEventScreen.jsx:362`). So a host who explicitly said "this event has no running order" still shows punters a message implying one is coming. | BUG (confirmed) | Small, isolated fix: gate the "coming soon" message on the event actually having day/slot data, not only on the publish toggle. Recommend for next dedicated polish pass — not in this authorized 3-item batch. |
| S22 | "Private set times" toggle should read as a positive "public" framing | **Found two distinct toggles in `CreateEventScreen.jsx:592-593`**: "Private set times" (artists only see their own slot — an artist-facing secrecy toggle) and "Show set times publicly" (punter-facing visibility toggle, already framed positively: on = public tab appears). The second toggle already matches what was asked for. The likely real issue is that two similarly-worded set-times toggles sit adjacent and are easy to conflate. | UX ambiguity — needs the user to confirm which toggle felt wrong, live | Logged; not clear-cut enough to fix blind. |
| S23 | What's On: "Postcode" search label should read "Where"/suburb-town wording | `WhatsOnScreen.jsx:350` hardcodes placeholder `"📍 Postcode"`. | POLISH (copy) | Simple copy change; logged for a small copy-only pass. |
| S24 | Venue profile view "vibes" pill has two colours in its gradient (inconsistent with other gradient pills) | Not yet located precisely — report was interrupted before the specific component was named. | UNCONFIRMED | Needs a repro (screenshot or which pill) before investigating further. |
| S25 | Profile Consolidation – Phase 2: migrate `VenueProfileScreen.jsx` onto the shared `useProfileForm`/`ProfileFormShell` architecture | Confirmed during the 2026-07-14 profile-screen consolidation commit — Artist/Band/Host/Standup all migrated; Venue was left with its own inline save/dirty/avatar/postcode logic, the last duplicated profile-editor implementation. | DEBT | Logged, not implemented. Remove the final duplicated implementation once migrated — no behavior change intended, pure consolidation. |
| S26 | Taxonomy Cleanup: resolve or remove the unused `PROFILE_FIELDS` scaffolding in `profileTaxonomy.js` | Confirmed dead — grepped zero imports anywhere in `v2/src`; it's stale, pre-dates the real profile-field names (`display_name`/`description`/`address`/`phone`/`accessibility`/`image` don't match the live schema). Feeds `export-profile-schema.mjs` -> Studio's `profile-schema.json`, but Studio's own SQL-export mapping (`sql-export-mapping.js`) was built against the live schema directly, not this. | DEBT | Logged. Either wire it up properly against real columns or delete it — don't leave dead/misleading architecture in place. |
| S27 | Dependency Cleanup: move the misplaced `@dnd-kit/*` dependency from the repo root `package.json` into `v2/package.json` | Confirmed during the same 2026-07-14 audit — `@dnd-kit/core|sortable|utilities` are declared in a stray root-level `package.json` and only physically installed in the root `node_modules`; `v2/package.json` has no such dependency at all, yet `EventScreen.jsx` imports `@dnd-kit` and currently only resolves by Node walking up to the parent directory. Works today by accident; would break on a clean clone, in CI, or if `v2/` is ever deployed standalone. | DEBT (real bug, not yet triggered) | Logged, not implemented. Move the dependency into `v2/package.json`, delete the stray root `package.json`, and verify a clean clone + `npm install` + `npm run build` succeeds from inside `v2/` alone (no reliance on parent-level `node_modules`). |
| S28 | ProfileScreen multi-profile follow picker inserts `p.type` strings (`'venue'`, `'artist'`) into `follows.user_id`, a `uuid` column | **Confirmed by code reading (2026-07-17), during the Active Profile Context UX review.** `ProfileScreen.jsx:841` keys `followSelected` by `p.type`; `:865` calls `doFollow([...followSelected])`; `:276-277` inserts each element as `user_id` → `invalid input syntax for type uuid`. **Not a new discovery** — `m2-design-review-2026-07.md:50` recorded it on 2026-07-11 and flagged it out of scope for M2; `known-issues/profile-follow-entity-type-drift.md:29-31` cross-references it. Logged here because it had never been surfaced into the active soak backlog, and because it is **stacked behind S1**: fixing this alone changes nothing, since S1's `entity_type: 'profile'` still fails the CHECK on the same insert. Both fail silently (`doFollow` never reads `.error`). **It cannot be repaired, only replaced** — every profile a user owns shares one `user_id`, `follows` has no `from_profile_id` until M6 (v1.1 §A6), and `:257-259` doesn't even select `profiles.id`. The picker asks a question the schema cannot represent; with uuids corrected it would write N identical duplicate rows. | BUG — **pre-M6 architectural mismatch, not data corruption** | **BACKLOG — do not fix now.** Nothing corrupt is written: the insert is *rejected*, so there are no bad rows to clean up. It is not an M6-BLOCKER — M6 is the fix, not the victim: §A6 adds `from_profile_id` and the picker becomes representable for the first time. Recommendation at M6: **remove the picker rather than repair it**, and fold the follow path into the shared "Sending as" control. Detail + rationale: `active-profile-ux-review-2026-07.md` F8 and §5.6 item 3. Pair with S1 when the follow path is reworked — fixing either alone leaves the button broken. |
| S29 | **Personal (`punter`) profiles are enumerable and readable by any unauthenticated caller** — v1.1 §A9 ("never publicly discoverable") is not enforced anywhere in the database | **Confirmed live 2026-07-17** by direct anon-key probes against production (`live-schema-audit-2026-07.md` §1.9). Holding only the public anon key that ships in the web bundle: `GET /profiles?type=eq.punter&select=id,type` → **200, 7 rows**; `select=name` → **display name returned**; `?id=eq.<punter id>` → **200, 1 row**. Corroborated by the recorded policy set: `profiles` carries two SELECT policies with `USING (true)` — `Anyone can read profiles` and `enable read for everyone` (`TO anon, authenticated`) — neither excluding `type='punter'` (`m0-audit:52-54`, re-verified byte-identical by M4's §7 pre-flight). **The `.neq('type','punter')` filter is client-side only**: it shapes what the app renders after the rows are already on the wire. v1.1 §A9 states Personal "already ships; v1.1 only documents it" — **it does not ship**. Scope: exposes display names + ids; **not** credentials, contact details or messages. | **SECURITY** (production RLS defect) | **REMEDIATE ON ITS OWN SCHEDULE — independent of M6.** Not an M6 dependency and not an enhancement: M6 does not touch this and will not fix it. The fix is an RLS policy on `profiles` restricting `type='punter'` reads to the owner, as its own isolated migration. **The CI spec's C6 does NOT close this** — C6 centralises the client filter into `profileResolution.js`; centralising all five copies would not remove a single row from the API response. C6 remains worth doing for consistency, but must not be recorded as satisfying §A9. Amend C6's claim when this is scheduled. Detail: `live-schema-audit-2026-07.md` §1.9, D3. |
| S30 | `personal_events` returns **500 on every query** — infinite recursion in its RLS policy. **Not a Supabase outage.** | **Confirmed live 2026-07-17, reproduced twice consecutively** (`live-schema-audit-2026-07.md` §1.8): `GET /personal_events?limit=0` → `500 {"code":"42P17","message":"infinite recursion detected in policy for relation \"personal_events\""}`. `42P17` is a deterministic policy-definition fault — a policy referencing its own table such that evaluation re-triggers it. It is not transient and **will not self-resolve**. This refutes the standing hypothesis (memory `supabase-pending`) that the 500s were caused by "a documented technical outage" and "may self-resolve"; that note was written during an outage window and the correlation was coincidental. Context: `personal_events` has **zero DDL in the repo** — no `CREATE TABLE`, no `ALTER`, no policy migration — so the offending policy exists only in the dashboard and cannot be read from the repository. | BUG (confirmed, production) | **BACKLOG.** The My Scene "add to your scene" personal-events feature is entirely non-functional in production — every read fails. Not a security issue (it fails closed) and not M6-related. Fix requires reading the live policy (`SELECT policyname, qual, with_check FROM pg_policies WHERE tablename='personal_events'`) and rewriting the recursive clause — needs SQL-editor access. Retire the outage hypothesis in `supabase-pending` when scheduled. |

### Global polish pass — completed 2026-07-12

Authorized as a scoped, 3-item GLOBAL/COMMUNAL pass (no architecture changes, no unrelated refactors, no business-logic changes). Landed as one isolated commit.

1. **Safe-area rule** (closes S15) — `--yp-safe-bottom` CSS var, live-updated by `BottomNav` and the mini-player mount, consumed by every bottom-docked sheet/modal/popup app-wide.
2. **Horizontal-scroll discoverability** (not separately reported; issued as a standing rule alongside S15) — `useDragScroll(discoveryId)` now performs a one-time subtle scroll bump on a rail's first appearance, for each user's first 10 visits per rail (persisted in `localStorage`), then stops permanently. Wired into all 12 existing horizontal-scroll call sites app-wide.
3. **Past Events filter** (closes S18) — shared `PastEventsSearch` component + `filterPastEvents` helper, reusing the app's existing plain-substring filter pattern, wired into all 5 Past Events surfaces.

All other soak findings (S16, S17, S19–S24) were deliberately left untouched — logged only, per the pass's scope.
